# 工作流 (WORK FLOW )

本文档定义了项目的工作流程，所有的feature都必须根据本文当来执行

1.  **新建任务时**：直接复制这三个文件（`research.md`,`plan.md`, `api.md`）到项目的 `feature/[年]` 目录下，重命名为对应任务名。
2.  **思考阶段**：先填满 `research.md`，验证 Claude 对代码库的理解。
3.  **规划阶段**：在 `plan.md` 里写方案，然后**反复标注**，直到你完全认可。
4.  **API 文档阶段决策**：
   - 如果本次开发涉及API变动，则进入API文档编写阶段
   - 如果本次开发不涉及API变动，则直接跳过API文档阶段，进入执行阶段
5.  **API 文档编写审阅阶段**：
   - 拷贝 `task/api.md` 并填充此次任务的API，编写完成进入审阅阶段
   - 在 api.md 中修改部分API设计直到完美
   - **重要提示**：API 文档的变动可能会导致计划（plan.md）部分代码的调整，如有修改需重新标注直至完美
   - 审阅完成前，批准前绝不执行后续操作
6.  **执行阶段**：只有在API文档（如涉及）和计划（plan.md）都完美后，才开始执行阶段。复制执行指令给 Claude，机械完成所有 Todo。
7.  **反馈阶段**：用最短的句子反馈，方向错误直接 `git revert` 重写计划。
---

## 深度研究阶段

分析需求 在使用`task/research.md`，在feature文件夹下创建 年/年月日.md 的需求分析报告 

### 1. 代码库深读范围
- 涉及模块/文件:
    - `[文件路径1]`
    - `[文件路径2]`
- 依赖关系:
    - [描述当前模块与其他模块的依赖]
- 已知约束:
    - [技术约束、性能约束、业务约束等]

---

### 2. 深度分析结果
> 由 Claude 生成，需验证是否真正理解系统

#### 2.1 核心逻辑梳理
- [ ] 核心流程:
  [用文字 / 伪代码描述核心执行流程]
  plaintext
- [ ] 关键数据结构:
  [列出关键类 / 结构体 / 表结构]
  plaintext

#### 2.2 风险点识别
- 潜在破坏点:
- [可能被本次修改影响的原有功能]
- 兼容性问题:
- [API 变更、数据库变更等兼容性风险]

---

### 3. 待确认问题
> 需人工/进一步调研确认的假设
1. [问题 1]
2. [问题 2]

---

### 4. 下一步
- 确认所有假设无误后，进入 `task/plan.md` 编写阶段
2. plan.md 模板（规划 + 标注 + 执行阶段）
   markdown
```md
# Plan: [任务名称]
> 日期: [YYYY-MM-DD]
> 关联: [链接到 research.md]
> 状态: 🔴 待标注 → 🟡 标注中 → 🟢 已批准 → 🚀 执行中 → ✅ 完成

---

## 1. 方案概述
### 1.1 目标
[清晰描述本次修改要达成的业务/技术目标]

### 1.2 方案选型
- 方案 A: [描述方案 A]
    - 优点: [...]
    - 缺点: [...]
- 方案 B: [描述方案 B]
    - 优点: [...]
    - 缺点: [...]
- **最终选择**: [方案 X]
    - 理由: [...]

---

## 2. 实现细节
### 2.1 修改文件清单
| 文件路径 | 操作类型 | 说明 |
|----------|----------|------|
| `[文件1路径]` | 新增/修改/删除 | [具体说明] |
| `[文件2路径]` | 新增/修改/删除 | [具体说明] |

### 2.2 关键代码片段（示例）
```java
// 这里放关键逻辑的伪代码/片段
// 由 Claude 生成，后续在 Annotate 阶段标注
public void sampleMethod() {
    // TODO: 实现逻辑
}
```
### 3.内联标注（Annotate 阶段核心）
> 在下方对方案 / 代码片段添加约束、纠正假设、拒绝方案
> 循环 1-6 次直至完美，批准前绝不执行

#### 3.1 标注示例
```txt
// use drizzle:generate, not raw SQL
// no – should be PATCH, not PUT
// remove entirely, no caching needed
→ "请处理全部注释并更新, don't implement yet"
```

#### 3.2 我的标注
[在此处添加你的内联标注，逐条修正方案]


### 4 API文档编写审阅阶段（可无）

> 标注结束以后，判断是否涉及API变动：
> - 涉及API变动：进入API文档编写审阅阶段
> - 不涉及API变动：直接跳过，进入Todo清单编写阶段

#### 4.1 API文档编写
> 拷贝 `task/api.md` 并填充此次任务的API，编写完成进入审阅阶段

#### 4.2 API文档标注审阅
> 在api.md中修改部分API设计直到完美。
> **重要提示**：API文档的变动可能会导致计划（plan.md）部分代码的调整，如有修改需重新标注直至完美。
> 审阅完成前，批准前绝不执行后续操作



### 5. Todo 清单（执行阶段）
> 颗粒度极细，每项可在 5-15 分钟内完成

 Task 1: [具体任务描述]
 Task 2: [具体任务描述]
 Task 3: [具体任务描述]

### 6. 执行指令（Implement 阶段）
标注完成并批准后，复制以下提示给 Claude 执行

```txt
implement it all.
mark completed in plan.
do not stop until all done.
no unnecessary comments.
continuously run typecheck...
```


### 7. 反馈记录（Feedback 阶段）

* [日期] [反馈内容，单句 / 截图描述]
* [日期] [反馈内容，单句 / 截图描述]

### 8. 最终状态
完成时间: [YYYY-MM-DD]
回滚记录: [如有 git revert，记录原因]
遗留问题: [待后续处理的问题]